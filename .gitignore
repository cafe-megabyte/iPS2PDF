# Object files
*.o

# Backups
*~

# MacOS metadata
*.DS_Store

# Xcode user-specific state
xcuserdata/
*.xcuserstate
xcschememanagement.plist

# ICC profiles are intentionally present in local and distributed builds, but
# must not be published with the source repository.
BundledResources/Profiles/*

# Local font packages are used by build scripts when present, but must not be
# published with the source repository.
BundledResources/PostScriptBase14/
font4/
